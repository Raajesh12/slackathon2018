INSERT INTO DRIVERS(WORKSPACE, MORNING_TIME, EVENING_TIME, location, MAX_CAPACITY, MORNING_SEATS, EVENING_SEATS) VALUES ('BULLSHIT', 9, 6, '501 Green St Urbana IL', 4, 4, 4);

INSERT INTO RIDERS(WORKSPACE, MORNING_TIME, EVENING_TIME, location) VALUES ('BULLSHIT', 9, 6, '501 Green St Urbana IL');

CREATE TABLE IF NOT EXISTS RIDERS(
ID SERIAL PRIMARY KEY,
WORKSPACE VARCHAR NOT NULL,
MORNING_TIME INT,
EVENING_TIME INT,
LOCATION VARCHAR,
DRIVER INT references DRIVERS(ID)
);

CREATE TABLE IF NOT EXISTS DRIVERS(
ID serial PRIMARY KEY,
WORKSPACE VARCHAR NOT NULL,
MORNING_TIME INT,
EVENING_TIME INT,
LOCATION VARCHAR,
MAX_CAPACITY INT,
MORNING_SEATS INT,
EVENING_SEATS INT
);

SELECT * FROM drivers WHERE workspace = 'BULLSHIT' AND MORNING_TIME = 9 AND M
ORNING_SEATS > 0;