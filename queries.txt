INSERT INTO DRIVERS(SLACK_ID, WORKSPACE, MORNING_TIME, EVENING_TIME, location, MAX_CAPACITY, MORNING_SEATS, EVENING_SEATS) VALUES ('Test_Driver', BULLSHIT', 9, 6, '501 Green St Urbana IL', 4, 4, 4);

INSERT INTO RIDERS(SLACK_ID, WORKSPACE, MORNING_TIME, EVENING_TIME, location) VALUES ('Test_Rider', 'Slackathon', 8, 6, '400 Market Street San Francisco, CA');

CREATE TABLE IF NOT EXISTS RIDERS(
SLACK_ID VARCHAR PRIMARY KEY,
WORKSPACE VARCHAR NOT NULL,
MORNING_TIME INT,
EVENING_TIME INT,
LOCATION VARCHAR,
DRIVER VARCHAR references DRIVERS(SLACK_ID)
);

CREATE TABLE IF NOT EXISTS DRIVERS(
SLACK_ID VARCHAR PRIMARY KEY,
WORKSPACE VARCHAR NOT NULL,
MORNING_TIME INT,
EVENING_TIME INT,
LOCATION VARCHAR,
MAX_CAPACITY INT,
MORNING_SEATS INT,
EVENING_SEATS INT
);